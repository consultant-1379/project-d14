export declare class Tile {
  private dom: {
    tile: HTMLElement;
    header: HTMLElement;
    actions: NodeListOf<Element>;
    id: string;
  };
  private events: {
    maximizeTile: CustomEvent;
    minimizeTile: CustomEvent;
  };

  constructor(element: HTMLElement);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private maximize(element: HTMLElement): void;

  private minimize(element: HTMLElement): void;

  private toggleFullScreen(element: HTMLElement): void;

  private setTooltipMessage(element: HTMLElement, message: string): void;

  private hide(): void;

  private show(): void;

  private hideNotSelf(event: Event): void;

  init(): void;

  destroy(): void;
}
