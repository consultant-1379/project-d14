export declare class Accordion {
  private dom: {
    accordion: HTMLElement,
    lis: NodeListOf<Element>
  };

  static toggleAccordion(li: HTMLElement): void;

  constructor(htmlElement: HTMLElement);

  private handleventListeners(type?: 'remove'): void;

  init(): void;

  destroy(): void;
}
