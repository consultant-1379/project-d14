export declare class MultiPanelTile {
  private dom: {
    multiPanelTile: HTMLElement;
    leftPanel: HTMLElement;
    midPanel: HTMLElement;
    rightPanel: HTMLElement;
    leftPanelTriggers: NodeListOf<Element>;
    rightPanelTriggers: NodeListOf<Element>;
  };
  private listeners: {
    toggleLeftPanel(): void;
    toggleRightPanel(): void;
  };
  private flags: {
    panelLeft: boolean;
    panelRight: boolean;
  };

  private updatePanelBehavior(): void;

  private addEventListeners(): void;

  private removeEventListeners(): void;

  constructor(element: HTMLElement);

  init(): void;

  destroy(): void;

  togglePanel(panel: HTMLElement): void;

  isPanelClosed(panel: HTMLElement): boolean;

  hidePanel(panel: HTMLElement): void;

  showPanel(panel: HTMLElement): void;
}
