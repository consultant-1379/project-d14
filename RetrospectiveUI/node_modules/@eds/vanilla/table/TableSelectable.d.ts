export declare class TableSelectable {
  private dom: {
    table: HTMLElement;
    rows: NodeListOf<Element>;
    bodyRows: NodeListOf<Element>;
    masterCheckbox: HTMLInputElement;
  };
  private listeners: {
    clickBodyRow(row: HTMLElement): void;
    clickMasterCheckbox(): void;
  };
  private settings: {
    selectRowOnFirstTDClick: boolean
  };
  private events: {
    tableRowsSelected: CustomEvent;
  };

  constructor(element: HTMLElement);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private toggleRowCheckbox(row: HTMLElement): void;

  private toggleAllCheckboxes(): void;

  private setMasterCheckboxState(): void;

  private getNumberOfSelectedRows(): number;

  private getNumberOfRows(): number;

  private selectRow(row: HTMLElement): void;

  private unselectRow(row: HTMLElement): void;

  private isSelectable(): boolean;

  init(): void;

  destroy(): void;
}
