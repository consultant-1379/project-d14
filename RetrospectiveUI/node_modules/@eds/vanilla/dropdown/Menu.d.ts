export declare class Menu {
  private dom: {
    menu: HTMLElement;
    items: NodeListOf<HTMLElement>;
    emptyMessage: HTMLElement;
    highlightItem: HTMLElement;
  };
  private max: number;

  constructor(HtmlElement: HTMLElement);

  private unHighlightItem(item: HTMLElement): void;

  private toggle(): void;

  private show(): void;

  private hide(): void;

  private limitDisplayMaxItems(): void;

  private showList(list: NodeListOf<HTMLElement>): void;

  private showEmptyState(): void;

  private hideEmptyState(): void;

  private unHighlightAll(): void;

  private getHighlightedItem(): HTMLElement;

  init(): void;
}

