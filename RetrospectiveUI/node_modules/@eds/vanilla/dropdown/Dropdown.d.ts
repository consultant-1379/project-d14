import { Menu } from './Menu';

export declare class Dropdown {
  private dom: {
    dropdown: HTMLElement;
    button: HTMLElement;
    checkboxes: NodeListOf<HTMLElement>;
    menu: HTMLElement;
    items: HTMLElement;
    input: HTMLElement;
  };
  private type: 'click' | 'single' | 'multi' | 'combo';
  private initialLabel: string;
  private isDisabled: boolean;
  private menu: Menu;
  private highlightIndex: number;

  constructor(HtmlElement: HTMLElement);

  private removeEventListeners(): void;

  private addEventListeners(): void;

  private setDisabled(): void;

  private arrowKeySelection(event: KeyboardEvent): void;

  private filterOptions(): string[];

  private setInputValue(target: HTMLElement): void;

  private selectSingleOption(event: Event): void;

  private selectMultipleOptions(): void;

  private ifClickedOutside(callback: Function, type?: 'remove'): void;

  init(): void;

  destroy(): void;
}
