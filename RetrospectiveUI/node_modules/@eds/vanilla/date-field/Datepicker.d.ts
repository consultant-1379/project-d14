import { Calendar } from './Calendar';

export declare class Datepicker {
  private dom: {
    datepicker: HTMLElement,
    calendar: HTMLElement,
    button: HTMLElement,
    input: HTMLElement,
    days: NodeListOf<HTMLTableDataCellElement>,
    hiddenInput: HTMLElement,
  };
  private listeners: {
    ifClickedOutside(event: Event): void,
    selectDay(event: Event): void,
    toggleCalendar(): void,
    listenInput(event: Event): void,
  };
  private calendar: Calendar;
  private selectedDate: {
    year: string,
    month: string,
    day: string,
  };

  constructor(element: HTMLElement);

  private removeEventListeners(): void;

  private addEventListeners(): void;

  private isValidDate(dateString: string): boolean;

  private listenInput(): void;

  private invalidDateFormat(): void;

  private setSelectedDateCSS(): void;

  private hide(): void;

  private show(): void;

  private toggleCalendar(): void;

  private ifClickedOutside(event: Event): void;

  init(): void;

  destroy(): void;

  selectDay(event: Event): void;
}
