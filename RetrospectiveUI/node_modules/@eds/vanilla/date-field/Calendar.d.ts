export declare class Calendar {
  private dom: {
    calendar: HTMLElement;
    year: HTMLElement;
    month: HTMLElement;
    days: NodeListOf<Element>;
    prevYear: HTMLElement;
    prevMonth: HTMLElement;
    nextMonth: HTMLElement;
    nextYear: HTMLElement;
    body: HTMLElement;
  };
  private eng: {
    months: string[];
    days: string[];
  };

  calendar: {
    year: string;
    month: string;
    day: string[];
  };

  constructor(htmlElement: HTMLElement);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private prevYear(): void;

  private prevMonth(): void;

  private nextMonth(): void;

  private nextYear(): void;

  private createCalendarBody(): void;

  private setDOMCalendar(): void;

  private getDayNum(day: string): string;

  private getMonthNum(day: string): string;

  setDataCalendar(date: Date): void;

  yyyymmdd(date: Date): string;

  formatDate(date: { year: string, month: string, day: string }): void;

  removeAllSelectedDays(): void;

  init(): void;

  destroy(): void;
}
