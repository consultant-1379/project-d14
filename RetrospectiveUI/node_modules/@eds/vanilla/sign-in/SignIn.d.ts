import { Input } from '@angular/core';

export declare class SignIn {
  private dom: {
    signin: HTMLElement;
    reveal: HTMLElement;
    inputPassword: HTMLInputElement;
    form: HTMLFormElement;
    error: HTMLElement;
    welcome: HTMLElement;
    loggedInUrl: string;
  };
  private listeners: {
    click(): void;
    blur(event: Event): void;
    submit(event: Event): void;
  };
  private messages: {
    valueMissing: string;
    typeMissing: {
      email: string;
      fallback: string;
    };
    patternMismatch: {
      signum: string;
      fallback: string;
    };
    fallback: string;
  };
  private props: {
    validFields: boolean[];
  };

  constructor(element: HTMLElement, loggedInUrl?: string);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private revealPassword(): void;

  private removeNativeValidation(): void;

  private validateFieldOnBlur(event: Event): void;

  private validateFormOnSubmit(event: Event): void;

  private getErrorType(field: HTMLInputElement): string;

  private successSignIn(): void;

  private showWelcomeScreen(): void;

  private hideSignInForm(): void;

  init(): void;

  destroy(): void;
}
