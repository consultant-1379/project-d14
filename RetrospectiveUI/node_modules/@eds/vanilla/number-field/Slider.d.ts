export declare class Slider {
  private dom: {
    slider: HTMLElement;
    input: HTMLElement;
    valueHolder: HTMLElement;
  };
  private listeners: {
    change(): void;
  };
  private properties: {
    oldValue: string;
  };

  constructor(element: HTMLElement);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private updateValueHolder(): void;

  init(): void;

  destroy(): void;
}
