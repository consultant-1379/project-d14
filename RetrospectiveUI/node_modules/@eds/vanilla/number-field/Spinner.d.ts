export declare class Spinner {
  dom: {
    spinner: HTMLElement;
    controls: NodeListOf<HTMLElement>;
    max: number;
    min: number;
    step: number;
    input: HTMLElement;
  };
  private type: string;
  private zeroLeading: string;
  private events: {
    changeTimerValue: CustomEvent;
  };

  constructor(element: HTMLElement);

  private addEventListeners(): void;

  private removeEventListeners(): void;

  private addLeadingZero(event: Event): void;

  private toggleTimeMode(): void;

  private setCorrectState(): void;

  private setCorrectFormat(): void;

  private increaseValue(): void;

  private leadingZero(number: number): number;

  init(): void;

  destroy(): void;
}
